#include <bits/stdc++.h>
using namespace std;
int main()
{
    cin.tie(0);
    //First, we take the numbers we're going to subtract:
    int A, B; cin >> A >> B;
    //Finally, we output the numbers after subtraction:
    cout << (A - B) << "\n";
}